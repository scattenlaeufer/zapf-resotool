#!/usr/bin/bash
set -x
DESTDIR=/srv/resotool

mkdir -p $DESTDIR
cd $DESTDIR || exit 1

cp -r "$SRCDIR"/* $DESTDIR

python3 -m pip install poetry
poetry config --local settings.virtualenvs.in-project true
poetry install --no-dev --no-interaction
